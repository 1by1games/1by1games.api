apiVersion: v1
kind: Service
metadata:
    name: "onebyoneaks-api"
    labels:
        app: "onebyoneaks-api"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "onebyoneaks-api"